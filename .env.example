# Database (PostgreSQL)
# Exemplo local: postgresql://postgres:postgres@localhost:5432/28webconnect_dev?schema=public
DATABASE_URL="postgresql://user:password@localhost:5432/28webconnect?schema=public"

# NextAuth.js
# NEXTAUTH_URL: URL base da aplicação (ex: http://localhost:3000 para desenvolvimento)
NEXTAUTH_URL="http://localhost:3000"
# NEXTAUTH_SECRET: Chave secreta para criptografia de tokens (gerar com: openssl rand -base64 32)
NEXTAUTH_SECRET="your-secret-key-here-generate-with-openssl-rand-base64-32"

# Mailgun - Serviço de Email Transacional
# MAILGUN_API_KEY: API key do Mailgun (obter em: https://app.mailgun.com/app/sending/domains)
MAILGUN_API_KEY="your-mailgun-api-key"
# MAILGUN_DOMAIN: Domínio verificado no Mailgun (ex: mg.28webconnect.com)
MAILGUN_DOMAIN="mg.yourdomain.com"
# MAILGUN_FROM_EMAIL: Email remetente (deve pertencer ao domínio configurado)
MAILGUN_FROM_EMAIL="noreply@28webconnect.com"
# MAILGUN_FROM_NAME: Nome exibido como remetente
MAILGUN_FROM_NAME="28Web Connect"

# Mistral AI - API para embeddings e chat
# Obter chave em: https://console.mistral.ai/api-keys/
MISTRAL_API_KEY="your-mistral-api-key"

# Groq - API para chat rápido e barato
# Obter chave em: https://console.groq.com/keys
GROQ_API_KEY="your-groq-api-key"

# Umami Analytics - Analytics privacy-friendly (opcional)
NEXT_PUBLIC_UMAMI_WEBSITE_ID="your-umami-website-id"
NEXT_PUBLIC_UMAMI_URL="https://analytics.yourdomain.com"

# Web Push (VAPID) - Notificações push (opcional)
# Gerar chaves com: npx tsx scripts/generate-vapid-keys.ts
# Ou: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY="your-vapid-public-key"
VAPID_PRIVATE_KEY="your-vapid-private-key"
# VAPID_SUBJECT: Email ou URL do responsável pelas notificações push
VAPID_SUBJECT="mailto:noreply@28webconnect.com"
# NEXT_PUBLIC_VAPID_PUBLIC_KEY: Mesma chave pública, exposta para o frontend
NEXT_PUBLIC_VAPID_PUBLIC_KEY="your-vapid-public-key"

# Redis - Cache e rate limiting (opcional)
REDIS_URL="redis://localhost:6379"

# Upload Storage - Configuração de armazenamento de arquivos
UPLOAD_DIR="./uploads"
MAX_UPLOAD_SIZE_MB="100"

# Base de Conhecimento - Configuração RAG
MAX_KNOWLEDGE_DOCUMENTS=1000
MAX_KNOWLEDGE_SIZE_MB=500
KNOWLEDGE_UPLOAD_DIR=uploads/knowledge

# Environment - Ambiente da aplicação (development, production, test)
NODE_ENV="development"

# Site Configuration
# NEXT_PUBLIC_SITE_URL: URL pública do site para links absolutos
NEXT_PUBLIC_SITE_URL="https://28webconnect.com"
# NEXT_PUBLIC_COMPANY_EMAIL: Email geral da empresa para contato
NEXT_PUBLIC_COMPANY_EMAIL="contato@28webconnect.com"
# NEXT_PUBLIC_DPO_EMAIL: Email do Data Protection Officer (LGPD)
NEXT_PUBLIC_DPO_EMAIL="seuemail@28webconnect.com"

# Contact Form
# CONTACT_EMAIL: Email que receberá mensagens do formulário de contato
CONTACT_EMAIL="contato@28webconnect.com"

# Payload CMS
# PAYLOAD_SECRET: Chave secreta para criptografia (gerar com: openssl rand -base64 32)
PAYLOAD_SECRET="your-payload-secret-key-generate-with-openssl-rand-base64-32"
# PAYLOAD_DATABASE_URI: URI do banco SQLite (mantido separado do PostgreSQL principal)
PAYLOAD_DATABASE_URI="file:./payload.db"

# Cron Job Security
CRON_SECRET=your-secure-random-token-here

# Data Retention (opcional - para alertas)
ALERT_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL
ALERT_EMAIL=admin@28webconnect.com
